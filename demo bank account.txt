<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🏦 MyBank Demo</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f2f2f2;
      text-align: center;
      margin: 0;
      padding: 0;
    }
    header {
      background: #0078d7;
      color: white;
      padding: 20px;
      font-size: 22px;
    }
    .account {
      background: white;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 300px;
      margin: 30px auto;
      padding: 20px;
    }
    img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      margin-top: 10px;
    }
    .name {
      font-weight: bold;
      font-size: 20px;
      margin-top: 5px;
    }
    .balance {
      margin: 10px 0;
      font-size: 18px;
    }
    button {
      margin: 5px;
      padding: 8px 12px;
      border: none;
      background: #0078d7;
      color: white;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background: #005fa3;
    }
    .history {
      text-align: left;
      background: #fafafa;
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 10px;
      margin-top: 10px;
      font-size: 14px;
      height: 120px;
      overflow-y: auto;
    }
  </style>
</head>
<body>

  <header>🏦 MyBank — Demo Account</header>

  <div class="account" id="makick">
    <img src="https://via.placeholder.com/80" alt="Makick">
    <div class="name">Makick</div>
    <div class="balance">Balance: <span id="makickBalance">5000</span>৳</div>
    <button onclick="deposit('makick')">Deposit</button>
    <button onclick="withdraw('makick')">Withdraw</button>
    <button onclick="sendMoney('makick','nomoni')">Send → Nomoni</button>
    <div class="history" id="makickHistory"></div>
  </div>

  <div class="account" id="nomoni">
    <img src="https://via.placeholder.com/80" alt="Nomoni">
    <div class="name">Nomoni</div>
    <div class="balance">Balance: <span id="nomoniBalance">3000</span>৳</div>
    <button onclick="deposit('nomoni')">Deposit</button>
    <button onclick="withdraw('nomoni')">Withdraw</button>
    <button onclick="sendMoney('nomoni','makick')">Send → Makick</button>
    <div class="history" id="nomoniHistory"></div>
  </div>

  <script>
    const balances = {
      makick: 5000,
      nomoni: 3000
    };

    function updateBalance(name) {
      document.getElementById(name + 'Balance').innerText = balances[name];
    }

    function deposit(name) {
      const amount = parseInt(prompt("কত টাকা জমা দিতে চাও?"));
      if (amount > 0) {
        balances[name] += amount;
        updateBalance(name);
        logHistory(name, `+${amount}৳ (Deposit)`);
      }
    }

    function withdraw(name) {
      const amount = parseInt(prompt("কত টাকা তুলবে?"));
      if (amount > 0 && amount <= balances[name]) {
        balances[name] -= amount;
        updateBalance(name);
        logHistory(name, `-${amount}৳ (Withdraw)`);
      } else {
        alert("পর্যাপ্ত ব্যালেন্স নেই!");
      }
    }

    function sendMoney(from, to) {
      const amount = parseInt(prompt(`কত টাকা ${to}-কে পাঠাবে?`));
      if (amount > 0 && amount <= balances[from]) {
        balances[from] -= amount;
        balances[to] += amount;
        updateBalance(from);
        updateBalance(to);
        logHistory(from, `-${amount}৳ (Sent to ${to})`);
        logHistory(to, `+${amount}৳ (Received from ${from})`);
      } else {
        alert("ব্যালেন্স যথেষ্ট নয়!");
      }
    }

    function logHistory(name, text) {
      const box = document.getElementById(name + "History");
      const line = document.createElement("div");
      line.textContent = text;
      box.prepend(line);
    }
  </script>

</body>
</html>
