<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🏦 MyBank</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #eef3f9;
      color: #222;
      padding: 20px;
    }
    .card {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 320px;
      margin: 20px auto;
    }
    img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 10px;
    }
    h2 {
      margin: 0;
    }
    button {
      background: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      margin: 5px;
      border-radius: 8px;
      cursor: pointer;
    }
    button:hover {
      background: #0056b3;
    }
    .history {
      text-align: left;
      margin-top: 20px;
      background: #f9f9f9;
      border-radius: 8px;
      padding: 10px;
      height: 100px;
      overflow-y: auto;
    }
  </style>
</head>
<body>
  <div class="card">
    <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="User">
    <h2>👤 Account Name: <b>Makick</b></h2>
    <h3>💰 Balance: <span id="balance">5000</span>৳</h3>

    <button onclick="deposit()">Deposit</button>
    <button onclick="withdraw()">Withdraw</button>
    <button onclick="send()">Send</button>

    <div class="history" id="history">
      <b>Transaction History:</b><br>
      +1000৳ (Deposit)<br>
      -500৳ (Withdraw)
    </div>
  </div>

  <script>
    let balance = 5000;
    const balanceEl = document.getElementById('balance');
    const historyEl = document.getElementById('history');

    function deposit() {
      let amount = prompt("Enter deposit amount:");
      if (amount && !isNaN(amount)) {
        balance += Number(amount);
        updateBalance(`+${amount}৳ (Deposit)`);
      }
    }

    function withdraw() {
      let amount = prompt("Enter withdraw amount:");
      if (amount && !isNaN(amount)) {
        if (Number(amount) <= balance) {
          balance -= Number(amount);
          updateBalance(`-${amount}৳ (Withdraw)`);
        } else {
          alert("Insufficient balance!");
        }
      }
    }

    function send() {
      let name = prompt("Enter recipient name:");
      let amount = prompt("Enter amount to send:");
      if (name && amount && !isNaN(amount)) {
        if (Number(amount) <= balance) {
          balance -= Number(amount);
          updateBalance(`-${amount}৳ Sent to ${name}`);
        } else {
          alert("Not enough balance!");
        }
      }
    }

    function updateBalance(text) {
      balanceEl.textContent = balance;
      historyEl.innerHTML += `<br>${text}`;
    }
  </script>
</body>
</html>
